---
---

<section class="min-h-screen flex items-center justify-center bg-[#020617] text-white">

  <div class="w-full max-w-md animate-fade">

    <div class="text-center mb-8">
      <div class="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-green-500/10">
        <span class="text-green-400 text-2xl">🔒</span>
      </div>

      <h1 class="text-3xl font-bold tracking-tight">Welcome back</h1>
      <p class="mt-2 text-slate-400">Sign in to your account to continue</p>
    </div>

    <div class="rounded-2xl border border-slate-800 bg-[#020617]/60 backdrop-blur-xl p-6 shadow-[0_0_40px_rgba(0,0,0,0.6)]">

      <div class="mb-4">
        <label class="block mb-2 text-sm text-slate-400">Email</label>

        <div class="relative">
          <input 
            type="email" 
            placeholder="you@example.com"
            class="w-full rounded-lg border border-slate-800 bg-[#020617] px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-green-500"
          />
        </div>
      </div>

      <div class="mb-5">
        <label class="block mb-2 text-sm text-slate-400">Password</label>

        <div class="relative">
          <input 
            id="password"
            type="password"
            placeholder="Enter your password"
            class="w-full rounded-lg border border-slate-800 bg-[#020617] px-4 py-3 pr-10 text-sm text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-green-500"
          />

          <button 
            id="togglePass"
            type="button"
            class="absolute right-3 top-1/2 -translate-y-1/2 text-slate-500 hover:text-white transition"
          >
            👁️
          </button>
        </div>
      </div>

      <button class="w-full bg-green-500 hover:bg-green-600 transition-all duration-300 py-3 rounded-lg font-semibold flex items-center justify-center gap-2 hover:scale-[1.02]">
        Sign in →
      </button>

      <p class="text-center text-sm text-slate-500 mt-4">
        Don't have an account?
        <span class="text-green-400 hover:underline cursor-pointer">Sign up</span>
      </p>

    </div>

  </div>

  <script>
    const btn = document.getElementById("togglePass");
    const input = document.getElementById("password");

    btn?.addEventListener("click", () => {
      input.type = input.type === "password" ? "text" : "password";
    });
  </script>

</section>